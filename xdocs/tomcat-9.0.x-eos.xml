<?xml version="1.0" encoding="UTF-8"?>
<document>

  <properties>
    <author>Apache Tomcat Project</author>
    <title>End of support for Apache Tomcat 9.0.x</title>
  </properties>

<body>

<section name="End of support for Apache Tomcat 9.0.x" rtext="2026-02-11">
<p>The Apache Tomcat<sup>&#174;</sup> team announces that support for Apache Tomcat 9.0.x
will end on 31 March 2027.</p>

<p>This means that after 31 March 2027:</p>
<ul>
  <li>releases from the 9.0.x branch are highly unlikely</li>
  <li>bugs affecting only the 9.0.x branch will not be addressed</li>
  <li>security vulnerability reports will not be checked against the 9.0.x
    branch</li>
</ul>

<p>Three months later (i.e. after 30 June 2027)</p>
<ul>
  <li>the 9.0.x download links will be removed</li>
  <li>the latest 9.0.x release will be removed from the mirror system</li>
  <li>the links to the 9.0.x documentation will be removed from
    tomcat.apache.org</li>
</ul>

<p>Note that all 9.0.x releases will always be available from the archive.</p>

<p>It is anticipated that the final 9.0.x release will be made shortly
before 31 March 2027.</p>

<p>Shortly before 9.0.x support ends, releases from a new 9.1.x branch will be
made available. Releases from the 9.1.x branch will continue until 31 December
2030.</p>

<p>The differences between 9.0.x and 9.1.x will be minimal and users can start
making the necessary transition now. Those changes are:</p>
<ul>
<li>the APR/native connectors for HTTP, HTTPS and AJP will not be available</li>
<li>Support for Tomcat Native 2.0.x will continue but Tomcat Native 1.3.x will
    not be supported (see the separate EOS announcement for Tomcat Native
    1.3.x)</li>
</ul>

<p>Users of the APR/native AJP connector are strongly encouraged to migrate to
the NIO AJP connector and discontinue using Tomcat Native.</p>

<p>Users of the APR/native HTTP connector are strongly encouraged to migrate to
the NIO HTTP connector and discontinue using Tomcat Native.</p>

<p>Users of the APR/native HTTPS connector are strongly encouraged to migrate to
either the the HTTPS NIO+JSSE connector or the HTTPS NIO+OpenSSL connector.
Those migrating to the HTTP NIO+OpenSSL connector should then upgrade Tomcat
Native to 2.0.x.</p>

<p>For users that are not using Tomcat Native or are using Tomcat Native 2.0.x,
the transition from 9.0.x to 9.1.x will be no different to a normal Tomcat point
release upgrade.</p>

<p>Rather than continuing on 9.1.x, Tomcat 9 users are strongly encouraged to
consider upgrading to a more recent Tomcat version making use of tools like the
Tomcat Migration Tool for Jakarta EE.</p>

</section>

</body>
</document>

